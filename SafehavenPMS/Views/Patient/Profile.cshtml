@model SafehavenPMS.ViewModel.ClinicalStaffProfileViewModel


@{
    var patient = Model.Patients.FirstOrDefault(); // safely get the first staff member
    ViewBag.Title = patient != null ? $"{patient.Firstname} {patient.Lastname} - Profile" : "Staff Profile";
}
<div class="container-fluid">
    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-2">
        <div class="d-flex align-items-center">
            <a href="@Url.Action("Index", "Patient")" class="me-3">
                <i class="bx bx-reply fs-1 text-custom"></i>
            </a>
            <img src="@(string.IsNullOrEmpty(patient?.PhotoUrl) ? "/Images/default.png" : patient?.PhotoUrl)"
                 class="rounded-circle me-3" style="width:60px; height:60px;" />
            <div>
                <div class="d-flex flex-row justify-content-start align-items-center" style="gap:10px">
                    <h4 class="mb-0 text-custom">@patient?.Firstname @patient?.Lastname</h4>
                    <span class="badge bg-success">@(patient?.PatientStatus)</span>
                </div>
                <p class="mb-0 text-muted">@patient?.PatientId</p>
            </div>
        </div>
    </div>

    @* Tabs *@
    <ul class="nav nav-tabs mb-4" id="staffTab" role="tablist">
        <li class="nav-item" role="presentation">
            <button class="nav-link active" id="overview-tab" data-bs-toggle="tab" data-bs-target="#overview"
                    type="button" role="tab" aria-controls="overview" aria-selected="true">
                Overview
            </button>
        </li>
        <li class="nav-item" role="presentation">
            <button class="nav-link" id="personal-tab" data-bs-toggle="tab" data-bs-target="#personal"
                    type="button" role="tab" aria-controls="personal" aria-selected="false">
                Personal
            </button>
        </li>
        <li class="nav-item" role="presentation">
            <button class="nav-link" id="treatment-tab" data-bs-toggle="tab" data-bs-target="#treatment"
                    type="button" role="tab" aria-controls="treatment" aria-selected="false">
                Treatment Details
            </button>
        </li>
        <li class="nav-item" role="presentation">
            <button class="nav-link" id="activity-tab" data-bs-toggle="tab" data-bs-target="#activity"
                    type="button" role="tab" aria-controls="activity" aria-selected="false">
                Activity Logs
            </button>
        </li>
    </ul>


    @* Tab Content *@
    <div class="tab-content" id="staffTabContent">
        <div class="tab-pane fade show active" id="overview" role="tabpanel" aria-labelledby="overview-tab">
            @await Html.PartialAsync("_OverviewTab", Model)
        </div>

        <div class="tab-pane fade" id="personal" role="tabpanel" aria-labelledby="personal-tab">
            @await Html.PartialAsync("_PersonalTab", Model)
        </div>

        <div class="tab-pane fade" id="treatment" role="tabpanel" aria-labelledby="treatment-tab">
            @await Html.PartialAsync("_TreatmentDetailsTab", Model)
        </div>

        <div class="tab-pane fade" id="activity" role="tabpanel" aria-labelledby="activity-tab">
            @await Html.PartialAsync("_ActivityLogsTab", Model)
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>


<style>
    .card {
        border: 1px solid #e9ecef;
        border-radius: 10px;
    }

    .nav-tabs .nav-link {
        color: #767676;
        background-color: transparent;
        border: none;
        font-weight: normal;
    }

        .nav-tabs .nav-link.active {
            color: #00A3B4;
            background-color: transparent;
            border: none;
            border-bottom: 3px solid #00A3B4;
            font-weight: bold;
        }

</style>
